plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.devopsadventure.maravillas360"
    compileSdk = 33 
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17 // Cambiado a VERSION_17
        targetCompatibility = JavaVersion.VERSION_17 // Cambiado a VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17 // Cambiado a VERSION_17
    }

    defaultConfig {
        applicationId = "com.devopsadventure.maravillas360"
        minSdk = 23
        targetSdk = 33
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
